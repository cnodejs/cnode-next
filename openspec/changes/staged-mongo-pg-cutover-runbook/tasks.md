## 1. Compose 迁移执行面准备

- [ ] 1.1 新增并提交 docker-compose 生产编排文件（至少包含 api、postgres、redis，支持一次性迁移任务容器）
- [ ] 1.2 为迁移任务定义 compose 内执行命令（建表、全量迁移、对账）并写入运维手册
- [ ] 1.3 在预发布环境验证 compose 内网络解析（服务名连通 postgres/redis）
- [ ] 1.4 验证步骤：在 compose 内成功执行 PostgreSQL 建表并检查表存在

## 2. 本地连接远程主机彩排链路

- [ ] 2.1 定义本地到远程主机的固定连接方案（SSH 隧道或内网跳板）并写入手册
- [ ] 2.2 为远程 Mongo 配置只读迁移账号，验证不可写源库
- [ ] 2.3 在本地执行一次完整迁移彩排（远程读源 + 本地/预发布写目标）
- [ ] 2.4 验证步骤：彩排后输出迁移耗时、对账结果和烟测结果
- [ ] 2.5 产出本地到远程彩排 Runbook（含命令模板、变量说明、校验点）
- [ ] 2.6 验证步骤：由另一位成员按 Runbook 独立执行并成功复现

## 3. 全量迁移脚本与重复运行能力

- [ ] 3.1 实现 `scripts/migrate-mongo-to-pg.ts`（users/topics/replies/messages/topic_collects/reply_ups）
- [ ] 3.2 实现 ObjectId 到 BIGINT 映射缓存策略，保证同一轮迁移引用一致
- [ ] 3.3 实现重复全量重跑能力（并行期每日可重跑，不需手工重建表）
- [ ] 3.4 验证步骤：连续两次全量迁移后，核心计数与抽样结果一致

## 4. 对账闸门与烟测

- [ ] 4.1 实现标准对账脚本或 SQL 模板（users/topics/replies/messages/reply_ups）
- [ ] 4.2 定义并固化关键烟测路径（登录、发帖、回复、消息）
- [ ] 4.3 产出每次迁移的对账报告（可读的通过/失败结论）
- [ ] 4.4 验证步骤：任一对账失败时流程阻断，不允许切流

## 5. 切换日 Runbook

- [ ] 5.1 编写分钟级切换日手册（停写、最终全量、对账、切流、下线）
- [ ] 5.2 明确并行期内测写入为可覆盖数据并同步给测试团队
- [ ] 5.3 完成至少一次完整彩排并记录耗时（迁移、对账、烟测）
- [ ] 5.4 验证步骤：彩排流程可在目标停机窗口内完成
