## 1. MVP：安全模板基础

- [x] 1.1 在 `apps/api/src/lib/mail.ts` 附近实现类型化的邮件输出与 HTML 文本、属性转义辅助函数
- [x] 1.2 实现仅接受 `http:`/`https:` 的目标 URL 校验，以及基于 `APP_WEB_BASE_URL` 和 URL 编码 retrieve key 的认证链接构造
- [x] 1.3 实现邮件客户端兼容的共享品牌布局，包含官方 CNode logo、文字品牌、内联样式、内容卡片、主 CTA、原始链接和 footer
- [x] 1.4 为长标题、回复内容和 URL 实现移动端可换行的安全摘要呈现，并确保用户 Markdown/HTML 不作为标记执行

## 2. MVP：四类邮件接入

- [x] 2.1 将账号激活邮件接入共享模板，同时生成包含完整激活 URL 的 HTML 和纯文本版本
- [x] 2.2 将密码重置邮件接入共享模板，同时生成包含完整重置 URL 的 HTML 和纯文本版本
- [x] 2.3 将话题回复邮件接入共享模板，同时生成话题标题、回复摘要、话题 URL 的 HTML 和纯文本版本
- [x] 2.4 将 @ 提及邮件接入共享模板，同时生成话题标题、提及摘要、话题 URL 的 HTML 和纯文本版本
- [x] 2.5 保持四个现有 `send*Mail` 函数签名、调用方、用户偏好判断、消息去重与 `sendMail` 五次重试行为不变

## 3. 功能完整性与验证

- [x] 3.1 为四类纯模板构建函数增加单元测试，断言 subject、品牌元素、关键文案、CTA、原始链接和纯文本替代
- [x] 3.2 增加特殊字符、恶意 HTML、危险 URL、retrieve key URL 编码和尾部斜杠 base URL 的边界测试
- [x] 3.3 使用代表性的长标题、长摘要和长 URL 检查生成 HTML 的流式宽度与换行属性，不依赖外部 CSS 或 JavaScript
- [x] 3.4 运行 API 定向测试、项目 TypeScript 类型检查和 ESLint，并修复本次变更引入的问题
