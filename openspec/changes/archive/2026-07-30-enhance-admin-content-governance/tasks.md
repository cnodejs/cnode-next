## 1. 后台话题筛选 API

- [x] 1.1 扩展 `GET /api/v1/admin/topics` 参数解析，支持 `q`、`tab`、`visibility`、`flag`、`date_field`、`date_from`、`date_to`、`sort`、`page`、`limit`。
- [x] 1.2 用枚举白名单构建 SQL 条件和排序，避免未校验参数进入 SQL 片段。
- [x] 1.3 返回话题列表所需的 `lock`、`archived`、`visit_count`、`collect_count`、`update_at`、`last_reply_at` 字段。
- [x] 1.4 为话题筛选组合补充 API 测试，覆盖 tab、状态、标记、时间范围和排序。

## 2. 后台话题筛选 UI

- [x] 2.1 扩展 `/admin/topics` loader，将筛选和排序 URL 参数转发到后台 API。
- [x] 2.2 增加筛选表单，支持 tab、可见状态、运营标记、时间字段、起止时间和排序选择。
- [x] 2.3 更新话题表格展示，补充锁定、归档、浏览数、收藏数、更新时间或最后回复时间等字段。
- [x] 2.4 确保分页、搜索、筛选、重置和批量操作后保留或正确清除 URL 筛选状态。

## 3. 巡检任务归类和任务级批量确认删除

- [x] 3.1 扩展 `GET /api/v1/admin/moderation` 支持 `job_id` 过滤，并在返回命中记录中包含 `scan_job_id`。
- [x] 3.2 增加任务级待处理命中统计，供任务卡片展示可批量确认删除数量。
- [x] 3.3 扩展或新增巡检批量处理接口，支持 admin 对某个 `job_id` 下的 pending 命中执行 `confirm`。
- [x] 3.4 任务级批量确认删除沿用现有话题/回复软删除、作者处罚、计数器维护和命中状态更新逻辑。
- [x] 3.5 任务级批量确认删除写入包含任务 ID、操作和实际处理数量的审计日志。
- [x] 3.6 更新 `/admin/mod` 任务卡片和命中列表，支持查看某任务命中和二次确认批量删除原始话题/回复。
- [x] 3.7 补充任务过滤、任务级批量确认删除、权限拒绝和已处理命中不重复处理的测试。

## 4. 后台话题真实删除

- [x] 4.1 增加 admin-only 真实删除 API，使用独立路由和明确的永久删除语义。
- [x] 4.2 在事务内按依赖顺序清理话题相关回复、回复点赞、收藏、招聘扩展、巡检命中、消息引用后删除话题。
- [x] 4.3 真实删除 API 写入审计日志，包含话题 ID、标题和清理结果摘要。
- [x] 4.4 在 `/admin/topics` 增加仅 admin 可见的真实删除入口和二次确认对话框，文案明确不可自动恢复。
- [x] 4.5 保持现有删除按钮为软删除，并确认巡检确认删除不调用真实删除。
- [x] 4.6 补充真实删除测试，覆盖权限、依赖清理、审计日志、取消确认无副作用和软删除语义不变。
- [x] 4.7 增加真实删除数据完整性检查，确认删除后不存在指向已删除话题或回复的收藏、回复点赞、招聘扩展、巡检命中或消息引用。

## 5. 文档和 Wiki 同步

- [x] 5.1 更新 `docs/content-moderation.md`，说明巡检结果支持按任务查看、任务级批量确认删除，以及该操作删除原始话题/回复但仍使用软删除生命周期。
- [x] 5.2 更新 `wiki/business-rules.md`，记录后台真实删除是 admin-only 独立危险操作，并区分作者/管理员软删除、巡检确认删除和数据库物理删除。
- [x] 5.3 确认 `wiki/legacy-behavior.md` 不需要更新；真实删除不是 legacy nodeclub 线上行为，不能写成兼容性要求。
- [x] 5.4 确认 `docs/database.md` 不需要更新；本 change 不包含 schema、索引、约束或迁移变更。
- [x] 5.5 检查 `design.md` 中删除语义图、依赖关系图和权限矩阵与最终实现、docs、wiki 保持一致。

## 6. 验证和收尾

- [x] 6.1 运行 `pnpm lint`。
- [x] 6.2 运行 `pnpm typecheck`。
- [x] 6.3 运行相关 API 和 Web 测试。
- [x] 6.4 运行 `openspec validate --changes enhance-admin-content-governance`。
- [x] 6.5 发布或 PR 前运行 `pnpm verify`。
