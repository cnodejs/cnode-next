## 1. 导航与入口信息架构

- [x] 1.1 调整主站桌面 Header：新增一级 `API` 入口，简化“关于”下拉为“新手指南 / 常见问题 / 关于我们”。
- [x] 1.2 同步移动端导航 sheet：移除“指引总览”，保留 `API`、新手指南、常见问题、关于我们。
- [x] 1.3 将用户下拉菜单和移动端用户入口中的“设置”统一改为“用户设置”。
- [x] 1.4 更新 CommandPalette 快捷入口命名和范围，避免继续暴露“指引总览”或“API 文档”旧命名。
- [x] 1.5 为 CommandPalette 增加权限过滤，确保非 admin/mod 用户看不到也搜不到“管理后台”或 `/admin` 入口。

## 2. 分页与基础控件密度

- [x] 2.1 扩展 `Pagination` 支持 simple/numbered 两种显示模式，并保持现有 numbered 行为默认不变。
- [x] 2.2 首页 topic feed 使用 simple pagination，只展示上一页/下一页并保留 tab query。
- [x] 2.3 确认后台、用户聚合页和招聘专区继续使用 numbered pagination，翻页保留筛选和搜索参数。
- [x] 2.4 收敛前台手写 `select`、`textarea` 和局部表单控件样式到基础 `Input` 密度，避免 `rounded-md`/`rounded-xl` 混用。
- [x] 2.5 为主站和后台长页面提供统一的“回到顶部”浮动按钮，仅在滚动较深时显示。

## 3. 写作和回复体验

- [x] 3.1 发布话题页为 MarkdownEditor 传入更高的页面级高度，并将发布按钮放入底部右侧操作区。
- [x] 3.2 编辑话题和编辑回复页面的保存/取消操作区对齐写作表单规范，主保存动作右侧展示。
- [x] 3.3 话题详情页“添加回复”点击后滚动到回复编辑器、清除定向目标并聚焦 Markdown 输入区。
- [x] 3.4 单条评论“回复”点击后设置引用目标、滚动到回复编辑器、聚焦输入区并保留 `@loginname`。
- [x] 3.5 话题详情页回复编辑器的“回复”提交按钮放在底部右侧操作区，定向回复和普通回复保持一致。

## 4. 话题详情 action surface

- [x] 4.1 重构 `TopicActions`：收藏/取消收藏作为主互动，查看回复作为页内导航，编辑/举报/管理动作归入更多或管理分组。
- [x] 4.2 确保管理动作中的删除、置顶、高亮等权限和确认流程不变，只调整展示层级。
- [x] 4.3 验证桌面和移动端 action surface 可发现、可点击，且不会与正文或回复区视觉混杂。

## 5. 招聘专区筛选条

- [x] 5.1 将 `JobFilterBar` 桌面筛选控件统一为工具栏密度，select、Input、清除按钮高度和圆角一致。
- [x] 5.2 保持移动端 Sheet 筛选行为和已选条件计数，确保触摸目标不小于 36px。
- [x] 5.3 验证地点、远程、薪资下限、技术栈筛选仍写入 URL query 并重置 page。

## 6. 管理后台概览与用户管理

- [x] 6.1 将管理概览顶部 8 张指标卡改为“社区规模 / 今日动态 / 待处理事项”三个 summary card。
- [x] 6.2 为待处理事项中的举报和巡检数字提供对应后台页面跳转，保持真实查询来源不变。
- [x] 6.3 将用户管理表格默认列调整为“用户 / 状态 / 角色 / 操作”，用户列合并展示 loginname 和 email。
- [x] 6.4 从用户管理默认表格列移除积分、话题数和回复数，保留搜索、分页、状态、角色和管理操作能力。
- [x] 6.5 检查后台表格内联编辑控件和筛选工具栏密度，确保 h-8/h-9 使用规则一致。

## 7. 验证与回归

- [x] 7.1 运行 `pnpm lint` 并修复与本 change 相关的问题。
- [x] 7.2 运行 `pnpm typecheck` 并修复类型错误。
- [x] 7.3 运行相关 Web 测试，至少覆盖 `Pagination`、`MarkdownEditor`、话题编辑入口和用户管理相关测试。
- [x] 7.4 手动检查 CommandPalette 在匿名、普通登录用户、admin/mod 三种身份下的入口过滤行为。
- [x] 7.5 手动检查桌面和移动端关键页面：首页、发布话题、话题详情长评论、招聘专区、管理概览、用户管理。
- [x] 7.6 确认本 change 未引入数据库、API 契约或文档同步要求变更。
