## 1. API 权限与状态写入

- [x] 1.1 梳理 `apps/api/src/routes/admin.ts` 现有用户、话题、回复管理接口，确认 block/unblock、delete topic、delete reply、top、good 的权限边界分别使用 `adminRequired` 或 `modRequired`。
- [x] 1.2 补齐用户页需要调用的 block/unblock API，确保仅 admin 可执行并写入审计日志。
- [x] 1.3 统一帖子删除写入语义，删除帖子时同时设置 `deleted=true` 和 `status='deleted'`，并确保公共列表和详情过滤已删除帖子。
- [x] 1.4 统一回复删除写入语义，删除回复时只标记目标回复为 deleted，并维护回复作者积分、回复数和话题回复数。
- [x] 1.5 确保置顶/取消置顶和高亮/取消高亮接口允许 admin/mod 执行，并写入审计日志。

## 2. 前台用户页管理入口

- [x] 2.1 扩展用户详情 API 或 Web loader，使用户页能获得当前登录用户的 admin/mod 状态和目标用户 block 状态。
- [x] 2.2 在 `apps/web/app/routes/user.$name.tsx` 添加仅 admin 可见的 block/unblock 操作入口。
- [x] 2.3 操作成功后展示明确反馈并刷新用户页数据，操作失败时展示后端错误。

## 3. 帖子详情页管理入口

- [x] 3.1 扩展帖子详情 API 或 Web loader，使帖子页能判断当前用户是否可执行内容管理动作。
- [x] 3.2 在帖子详情页添加 admin/mod 可见的“删除帖子”“置顶/取消置顶”“高亮/取消高亮”入口。
- [x] 3.3 确保删除帖子成功后当前帖子不再可见，并引导用户返回列表或显示已删除反馈。
- [x] 3.4 确保置顶和高亮操作成功后详情页和列表状态同步刷新。

## 4. 回复项管理入口

- [x] 4.1 在回复项渲染中添加 admin/mod 可见的“删除回复”入口，文案必须明确目标为回复。
- [x] 4.2 删除回复成功后刷新评论流或从当前列表移除该回复。
- [x] 4.3 确认删除回复不会删除所属帖子，所属帖子仍按自身状态决定是否可见。

## 5. 验证与回归

- [x] 5.1 新增或扩展验证脚本，覆盖 admin 用户页 block/unblock、非 admin 拒绝、admin/mod 删除帖子、删除回复、置顶和高亮。
- [x] 5.2 增加公共列表过滤验证，确认删除帖子不出现在所有帖子、无人回复、用户话题、用户回复聚合、用户收藏等公开出口。
- [x] 5.3 增加回复过滤验证，确认已删除回复不出现在帖子详情、最新回复和用户最新回复中。
- [x] 5.4 运行 `pnpm typecheck` 并确认通过。
- [x] 5.5 运行相关 API/Web smoke 验证并确认通过。

## 6. 部署检查

- [x] 6.1 部署 API/Web 后确认管理员在用户页可 block/unblock 用户。
- [x] 6.2 确认管理员或版主在帖子详情页可删除帖子、置顶和高亮。
- [x] 6.3 确认管理员或版主在回复项可删除回复，且删除回复不会让所属帖子从列表消失。
