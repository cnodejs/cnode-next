## 1. 编辑器 MVP

- [x] 1.1 将 `MarkdownEditor` 的预览状态从 boolean 改为 `edit` / `preview` / `split` 三种模式。
- [x] 1.2 实现编辑、预览、双栏编辑 + 预览的 toolbar 切换 UI，并保证键盘可访问。
- [x] 1.3 让预览模式和双栏预览区域统一复用 `MarkdownView` 渲染当前 Markdown。
- [x] 1.4 优化 textarea 选区插入逻辑，支持加粗、斜体、链接、代码、引用、列表、图片占位等常用 Markdown 操作。
- [x] 1.5 实现移动端双栏降级，窄屏下使用编辑/预览切换布局而不是强行双列。

## 2. 图片上传能力

- [x] 2.1 梳理现有 API 鉴权、OSS 配置和上传相关工具，确定上传接口放置位置。
- [x] 2.2 在 `apps/api` 增加登录用户图片上传接口，校验 MIME、扩展名和大小。
- [x] 2.3 接入阿里云 OSS 写入逻辑，上传成功后返回公开图片 URL 和原始文件名。
- [x] 2.4 在 web 侧增加上传客户端，处理成功、失败、未登录、文件过大、类型不支持等状态。
- [x] 2.5 在 `MarkdownEditor` 图片按钮中接入文件选择上传，成功后在当前光标插入 `![文件名](URL)`。
- [x] 2.6 支持 textarea 粘贴图片和拖拽图片上传，仅在检测到图片文件时拦截默认行为。

## 3. 路由集成与体验完善

- [x] 3.1 在 `topic.create.tsx`、`topic.$tid.edit.tsx`、`reply.$id.edit.tsx`、`topic.$tid.tsx` 验证增强后的编辑器调用方式。
- [x] 3.2 为上传中、上传失败、未登录上传、禁用态添加清晰反馈，失败时不得修改正文。
- [x] 3.3 确认 `MarkdownView` 对图片、表格、代码块在编辑器预览中具备响应式展示，不撑破移动端布局。
- [x] 3.4 保持现有表单提交流程不变，正文仍作为 Markdown 字符串提交。

## 4. 验证

- [x] 4.1 为 `MarkdownEditor` 增加或更新组件测试，覆盖三种模式、选区插入和预览复用。
- [x] 4.2 为图片上传 API 增加测试，覆盖未登录、非法类型、超限文件和成功上传。
- [x] 4.3 为粘贴/拖拽上传插入增加前端测试或可执行验证用例。
- [x] 4.4 运行 `pnpm typecheck`。
- [x] 4.5 运行相关测试命令或 `pnpm test`，记录无法运行的原因。
